/**
 * §G：空间访问校验的常驻说明（单一来源，TargetPicker / NewTaskView 共用）
 *
 * 口径与代码事实对齐（未做双账号实测前**不承诺**）：
 *  - 校验（validateTarget）只调用公开资料接口（cgi_userinfo_get_all），
 *    不打开对方空间页面 → 是否会被记入访客**无法确认**，文案如实写"请知悉"；
 *  - 备份采集时若开启「空间访问权限判断」（ZoneAccess，默认开）会逐个访问
 *    好友空间主页 → 与设置页文案（"关闭后不再产生访客记录"）同口径，写"可能留下访客记录"。
 */
export const VALIDATE_HELP_TEXT =
  '校验只读取对方的公开资料接口，不进入对方空间页面；是否会被记入访客无法确认，请知悉。';

export const BACKUP_ACCESS_NOTICE =
  '开始备份后若开启「访问好友空间判断权限」（默认开），会访问对方空间主页、可能留下访客记录' +
  '（可在 设置 → 内容默认设置 → 好友 中关闭）；日记、收藏等私密内容无法备份。';
