# v5.0.0 发布说明

## 本次修复

**v4.9.0 装机反馈三连修**

- **应用图标圆角真正生效**（v4.9 的圆角被源图锁死，256px 下半径仅占边长 2.7%，看上去仍是方的）：改为按输出尺寸 **15% 比例**圆角、先缩放后圆角、母图与成品分离；安装后任务栏 / 开始菜单圆角清晰可见
- **压缩档案失败不再"假成功"**：此前压缩失败也提示"已压缩"却找不到文件——现在失败会显示**具体原因**（最常见：目标文件已存在，请先处理该文件），成功提示附带**「打开所在文件夹」**按钮，压缩全程写入日志可查
- **校验说明文案纠正**（按双账号实测）：校验好友空间只读取对方公开资料接口、不进入空间页面，**不会留下访客记录**；备份采集若开启「访问好友空间判断权限」（默认开）仍会访问对方主页、可能留痕——两处提示保持分开、互不混淆

**缺陷修复**

- **修复「特别关心」标记丢失**：特别关心列表里包含非好友时程序会中断出错，导致其余好友的特别关心标记全部丢失——现在跳过异常条目、完整标记
- **顶栏 / 首页 / 本人卡片的「自己昵称」修好了**：引擎状态接口返回的 JSONP 文本此前没被解析，昵称永远读成空、界面一直回退显示 QQ 号（v4.9.0 起每次启动都如此）；顺带修复校验目标后资料卡昵称同样为空的问题

**其它**

- 打包配置显式声明发布渠道，修复纯净环境构建时 electron-builder 报 `Cannot read properties of null (reading 'channel')`

## 对应 issue

- [#1](https://github.com/salt-fishes/qzone-archiver/issues/1)：501（`main_page_cgi`）——来自备份好友时的逐好友空间权限判断，本版**未改动请求量**（优化已立项、需实测灰度）；临时可在 设置 → 内容默认设置 → 好友 中关闭「访问好友空间判断权限」
- [#3](https://github.com/salt-fishes/qzone-archiver/issues/3)：大媒体下载慢——延续 v4.7.0 的超时重试与明细过滤，大批量媒体建议搭配 Motrix 等下载器

## 下载

| 文件 | 说明 |
| --- | --- |
| `QZoneArchiver-5.0.0-setup.exe` | 安装版（推荐） |
| `QZoneArchiver-5.0.0-portable.exe` | 免安装版（首次启动需自解压，稍慢） |
