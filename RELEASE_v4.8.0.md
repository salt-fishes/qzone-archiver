# v4.8.0 发布说明

> 这是自 v4.6.0 以来的第一个正式发布：包含 v4.7.x 的全部累积改进 + 本次头像/表情渲染修复。

---

## 桌面版 v4.8.0

### 本次重点：头像与表情终于正常显示

v4.7.x 连续几个版本都在修这个，但一直没定位到根因。这次用真机复现 + 逐层实测，
把三个**互相独立**的原因都挖出来了：

**1. 头像永远是"文字头像"**

取头像的整条链路（本地缓存 → IPC → data URL）其实一直是好的，问题在界面写法上：
组件库的头像组件只要"文字占位"存在，就会只渲染文字、**完全忽略图片地址**。
所以无论头像有没有取到，界面都只显示昵称首字。已改为：有图时渲染图片、
加载失败或还没取到才回落首字；顶栏、目标选择卡片、我的档案三处头像也统一改成
走本地缓存（不再直连会被安全策略拦掉、且离线不可用的 qlogo 外链）。

**2. 昵称里的表情显示成原始代码**

昵称在 QQ 空间里是 `穗稔[em]e327806[/em]` 这种形式，界面上有 5 处地方直接按
纯文本输出（首页「最近任务」、备份确认页、完成页、我的档案分组、好友下拉列表），
于是用户看到的就是中括号代码；另外表情兜底链路也断了两截（内置表情目录路径少写
一级 `..`，以及表情图床被安全策略拦掉）。现在这些位置统一渲染成图片，
取不到图也不会再露出原始代码。

**3. 备份出来的档案里，内置表情整批缺失**

同一个"目录路径少一级"的问题，让内置表情一个都没被复制进备份产物；而采集引擎
对内置表情是跳过下载的（认为复制环节会补上）。于是老版经典表情（e1~e320）在
档案里**一个都没有**，魔法表情也因扩展名对不上而取不到图。

- 现在的备份会正确写入内置表情（含档案浏览器需要的 `.gif` 文件名别名）
- **已有老档案不用重新备份**：对同一个目录再跑一次备份即可补齐（复制是幂等的）

### 构建可复现（自行构建 / 二次开发）

表情资产与打包图标一直是"生成产物、不入库"，但两个生成脚本此前**没有接进任何
构建命令**（注释写着"构建前自动重建"，实际没接），导致在干净仓库里构建出来的包
本身就缺表情资产。现在：

- `npm run build:renderer` 会先同步表情资产
- `npm run dist:win` 会先生成图标，再构建、打包
- 打包守卫改为「配置 + 生成链路必须在位，产物存在时再校验尺寸/结构」，纯净仓库也能跑绿 CI

### 其它

- 新增 9 条回归测试（头像必须真渲染出图片、禁止"文字占位覆盖图片地址"的写法、
  表情目录/复制/别名/内联取图/安全策略、纯净仓库构建守卫），冒烟清单补 3 条人工用例
- 版本号同步（桌面版 / 扩展版 / 文档），清理仓库内已失效内容

---

## v4.7.x 累积改进（如果你还在 v4.6.0，请直接升级本版本）

### ⚠️ 免安装版（portable）启动修复

v4.6.0 的 `QZoneArchiver-4.6.0-portable.exe` 启动会直接报错：

```
A JavaScript error occurred in the main process
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '...\ipc-contract.mjs'
```

原因是打包白名单漏了 `src/shared` 目录（IPC 契约在主进程与 preload 之间共用）。
**用免安装版的话请务必升级**；安装版（setup）不受影响。已补静态守卫，同类漏配 CI 直接拦下。

### 下载改进（媒体文件多的情况）

- 修复「下载并发数」「下载间隔」设置不生效（v4.6 里界面上可改，实际被写死为 10 并发无间隔）
- 下载请求补齐超时（建连/首字节 30 秒、空闲 60 秒），卡住不再一直占并发位
- 单文件失败自动重试最多 5 次（指数退避），取消/暂停不触发重试
- 下载明细只显示多媒体文件（图片/视频/音频），并改为「文件名（模块名）」
- 明细列表虚拟滚动，上万条媒体不再卡界面

### 降低被频率限制（501）的概率

- 好友空间权限判断改为分批 + 随机间隔，不再对几百上千好友集中请求同一域名
- 接口连续异常达 5 次自动熔断并提示等待
- 「恢复已删除说说」在接口不可用时不再静默跳过

### 其它

- 应用自有图标（exe / 快捷方式 / 任务栏 / 安装向导）
- 拦截拖放：拖入文件或链接不再出现 `file://` 提示或直接跳转
- 扫码登录成功后界面自动刷新；新增图文教程与分步引导
- 清理两项无效设置：「隐藏浏览器下载栏」「Referer 域名」（桌面端已无对应实现）

---

### 说明

- Windows 10+ x64；Mac 用户请使用扩展版
- 扩展版本次仅同步版本号（v4.8.0），功能不再新增，维护集中在桌面版
- 媒体文件上万条时下载本身就慢（并发 10），建议按模块分批备份，单轮媒体量控制在几百条以内
