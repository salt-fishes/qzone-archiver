# v4.8.0 发布说明

## 本次修复（v4.8.0）

- **头像只显示"文字头像"**：界面写法会忽略头像图片地址，取图链路再通也出不了图 → 已修
- **昵称里的表情显示成原文**（如 `穗稔[em]e327806[/em]`）→ 已修，现在渲染为表情图片
- **备份产物里内置表情整批缺失** → 已修；老档案不用重备，对同一目录再跑一次备份即可补齐

## v4.7.x 修复

- **免安装版（portable）启动即报错** `Cannot find module '...ipc-contract.mjs'` → 已修（安装版不受影响）
- **持续 501（QQ 空间服务端限制）**：好友权限判断改为分批 + 随机间隔，连续异常自动熔断 → 大幅缓解
- **下载任务长时间没反应**：补齐建连/首字节超时，单文件失败自动重试，下载明细只显示媒体文件 → 已改善
- **备份头像不显示**：失败后不再写入空缓存，改为冷却后自动重试 → 已修
- **表情按真实文件名取用**：542 个表情里有 224 个是 `.png`，此前全链路写死 `.gif` → 已修
- 应用自有图标（exe / 快捷方式 / 任务栏 / 安装向导）
- 窗口禁止拖放；扫码登录后界面自动刷新并给出最小化提示
- 清理两项无效设置（「隐藏浏览器下载栏」「Referer 域名」）

## 对应 issue

- #4 免安装版启动失败 —— 已修复
- #1 #2 持续 501 —— 已缓解（分批 + 熔断；仍可能触发，属服务端频率限制）
- #3 最后一步下载任务没反应 —— 已改善（超时 + 重试 + 明细可见）

## 下载

Windows 10+ x64（Mac 用户请使用扩展版）：

- 安装版 `QZoneArchiver-4.8.0-setup.exe`
- 免安装版 `QZoneArchiver-4.8.0-portable.exe`
