# v4.7.0 发布说明

> 本文件用于 GitHub Release 正文，以及 issue #1 / #2 / #3 的回复，可直接复制粘贴。

---

## GitHub Release 正文（建议直接用下面这段）

## 桌面版 v4.7.0

### ⚠️ 重要：修复免安装版（portable）无法启动

v4.6.0 的 `QZoneArchiver-4.6.0-portable.exe` 启动会直接报错：

```
A JavaScript error occurred in the main process
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '...\ipc-contract.mjs'
```

原因是打包白名单漏了 `src/shared` 目录（IPC 契约在主进程与 preload 之间共用），
**如果你在用免安装版，请务必升级到本版本**；安装版（setup）不受影响。

已补上静态守卫测试，今后同类漏配会在 CI 直接拦下。

### 下载改进（媒体文件多的情况）

- 修复「下载并发数」「下载间隔」设置不生效的问题 —— v4.6 里这两项在界面上可改，
  实际却被写死为 10 并发且无间隔
- 下载请求补齐超时（建连/首字节 30 秒、空闲 60 秒）：此前若卡在连接阶段会一直占着
  并发位，媒体量大时整个下载队列会看起来"没反应"
- 单文件失败自动重试最多 5 次（指数退避），取消/暂停不触发重试
- 下载明细只显示**多媒体文件**（图片/视频/音频）：此前按目录判断，会把说说语音、
  日志附件等非媒体一并算进来
- 明细显示改为「文件名（模块名）」；新增「媒体下载中 x」提示，采集完成但媒体还在
  下载时不再无从判断
- 明细列表改为虚拟滚动，上万条媒体不再卡界面

### 降低被频率限制（501）的概率

- 好友空间权限判断改为分批 + 随机间隔：此前会对每个好友连打空间主页请求，
  几百上千好友会在几分钟内集中请求同一域名，这是导致 501 的直接诱因
- 接口连续异常达 5 次自动熔断并提示等待，不再持续重试加重限制
- 「恢复已删除说说」在接口不可用时不再静默跳过（此前会显示备份成功但实际没恢复）

### 其它

- 清理两项无效设置：「隐藏浏览器下载栏」「Referer 域名」（桌面端已无对应实现，
  保留只会造成误解）
- 新增回归测试：打包文件覆盖性、设置项读取点（防止再出现"改了没反应"的设置）

### 说明

- Windows 10+ x64；Mac 用户请使用扩展版
- 扩展版本次仅同步版本号（v4.7.0），功能不再新增，维护集中在桌面版

---

## issue #3 回复（最后一步下载任务到浏览器长时间没反应）

> 感谢反馈，v4.7.0 已针对这个场景修了几处：
>
> 1. **卡住不再无声无息**：下载请求此前没有超时保护，卡在连接阶段会一直占着并发位，媒体量大时整队看起来就像"没反应"；现在有超时 + 自动重试（最多 5 次）。
> 2. **「下载并发数 / 下载间隔」现在真的生效了** —— 之前这两项在界面上可改，实际写死 10 并发且无间隔，这是我们的问题。
> 3. **进度看得见了**：采集完成但媒体还在下载时，界面会显示「媒体下载中 x」；明细列表也不再因为上万条而卡顿。
>
> 另外说明一下：1 万多条媒体本身就需要较长时间，这是量级决定的，不完全是 bug。**建议按模块分批备份**（例如先只备份说说，再单独备份相册），单轮媒体量控制在几百条以内，速度和稳定性都会明显更好。
>
> 如果升级 v4.7.0 后仍卡住，麻烦把「运行日志」导出发我，并说明卡住时下载明细里排队/下载中的条数。

---

## issue #1 回复（持续 501：cgi-bin/main_page_cgi）

> 判断是**账号/网络侧的访问频率限制**，不是接口本身坏了 —— 这两个接口我们实测都能正常访问（返回 200，只有"未登录"提示），而 501 是成片出现在同一账号、跨越两个不同域的接口上。
>
> 也有我们请求偏猛的原因：好友模块会对**每个好友**发一次空间主页请求来判断其空间是否可见，这个循环里此前没有任何间隔，几百上千好友会在几分钟内集中请求同一域名。v4.7.0 已改为分批 + 随机间隔，并加了「连续异常自动熔断」，避免持续重试加重限制。
>
> 现在可以这样试：**暂停备份，等 1~2 小时或换个网络**，然后先只备份说说看是否恢复；不要短时间内反复重跑完整备份。
>
> 如果升级 v4.7.0 并等待后仍然持续 501，麻烦补充：应用版本、同一时刻 QQ 空间网页版能否正常打开。

---

## issue #2 回复（feeds2_html_pav_all 501）

> 与 #1 是同一问题（同一个人、同一时间段），合并处理，处理方式见 #1。
>
> 补充一点：这个接口不可用时，v4.6 及更早版本的「恢复已删除说说」会**静默跳过** —— 既不出错也不计入失败，备份报告仍显示成功。v4.7.0 已修正：接口不可用会明确报为失败，不会再假装成功。
