# v4.9.0 发布说明

## 本次修复

**修复**

- 登录后首页问候语、顶栏、备份界面本人卡片三处**都能显示昵称**了；昵称未就绪时显示 `QQ 号`，不再出现"你好"后面空着、或卡片上下两行都是 QQ 号的情况（#2/#7 同源问题）
- **退出登录现在会关闭 QQ 空间窗口**；手动关掉 QQ 空间窗口后，顶栏 1 秒内自动变为「未连接/连接失败 · 点击重试」，点一下即可重新拉起窗口，不再需要重启应用（#4/#8）
- 非好友不再被称作「好友」：「我的档案」更名为**「档案列表」**，向导入口更名**「访问空间」**；手输 QQ 号备份后标题显示 `QQ 号`、标签显示「他人空间」，真好友仍显示「好友」（#5）
- **修复下载队列静默冻结**（实机验证发现）：大文件（视频）下载完成后并发槽位未正确归还，队列从此永远停在"排队中"且无任何报错——此前表现为"备份完成了但图片/视频都没下载"
- **修复总进度倒退**：刚取完列表进入取全文阶段时，总进度不再退回 0%
- **修复完成页数据串场**：备份完成页显示的是上一次备份的档案；文件数/占用空间现在只统计本次任务实际写入的内容
- 顶栏头像昵称始终显示**登录者本人**（此前备份他人空间时会变成对方）

**新增 / 优化**

- **主题跟随系统**：顶栏主题按钮改为三选下拉（跟随系统 / 浅色 / 深色），选「跟随系统」后 Windows 切换深浅色界面自动跟随（#1）
- **扫码前提示不再被遮挡**：原来的两条提示合并为一个弹窗，直接显示在 QQ 空间窗口正上方，点「我知道了」才开始扫码；登录成功提示也改到窗口最小化之后显示，不再 3 秒闪没（#3）
- **应用图标改为圆角矩形**（Windows 11 规范分档半径），exe / 任务栏 / 开始菜单 / 窗口 / 首页五处一致（#6）
- **免安装版新增启动画面**：自解压期间显示品牌 splash，不再"点了没反应"（日常使用仍推荐安装版）
- 未登录首页标题改为**打字机循环文案**；首页背景新增缓慢流动的色斑动效与鼠标轻微互动——系统开启「减少动态效果」时自动关闭
- **日志双轨 + 分天 + 自动清理**：应用日志按天分文件（`logs/main-日期.log`），每次备份的任务日志独立落盘（`logs/backup-<任务号>.log`），过期自动清理；设置 → 高级 → 「打开日志目录」一键直达
- **下载列表按任务隔离**：新备份启动时自动清掉上一任务的下载记录，列表不再出现几千条旧媒体的"幽灵任务"
- **档案列表 / 最近备份可删除单条记录**；备份目录被移动或删除后自动标注「目录已不存在」并禁用浏览/压缩
- 新建备份向导勾选「相册」后可**挑选要备份的相册**（仅本人模式）
- 空间访问校验判定透明化：校验只读取对方公开资料接口、不进入对方空间页面；开始备份后若开启「访问好友空间判断权限」可能留下访客记录（可在 设置 → 内容默认设置 → 好友 中关闭）

## 移除「已删除说说恢复」（实验性功能）

该功能实际无法恢复任何说说（接口已不可用，市面同类"恢复"也基本为虚假宣传），本版本将其**移除**——此前提示"恢复 0 条"不是你操作的问题。已备份过的档案不受影响、仍可正常浏览；旧配置里残留的开关会被自动忽略。

## 扩展端已停止维护

本仓库的扩展端 fork 停留在上游 V2 时代的代码结构，而 [ShunCai/QZoneExport](https://github.com/ShunCai/QZoneExport) 已在其 V2 结构基础上更新到 **V3.0**（Manifest V3，WXT + TypeScript，含直写目录、断点续传与全新离线查看器），功能与维护状态均优于本仓库归档版本 —— **请改用上游扩展**（其新查看器可一键升级我们旧版扩展的备份）。

本仓库此后只维护 **Windows 桌面端**；历史扩展源码保存在 `extension` 分支（tag：`archive/extension-v4.8.0`），仅供追溯。

## 对应 issue

- [#4](https://github.com/salt-fishes/qzone-archiver/issues/4)：v4.6.0 安装包缺文件导致启动失败——已在 v4.7.0 修复，随本版确认关闭
- [#5](https://github.com/salt-fishes/qzone-archiver/issues/5)：已删除说说恢复——已按承诺随本版移除该实验性功能
- [#2](https://github.com/salt-fishes/qzone-archiver/issues/2)：501（`feeds2_html_pav_all`）——该接口随「已删除说说恢复」一并移除，此路径彻底消失
- [#1](https://github.com/salt-fishes/qzone-archiver/issues/1)：501（`main_page_cgi`）——来自备份好友时的逐好友空间权限判断，将在后续版本优化请求量；临时可在 设置 → 内容默认设置 → 好友 中关闭「访问好友空间判断权限」
- [#3](https://github.com/salt-fishes/qzone-archiver/issues/3)：大媒体列表下载慢——v4.7.0 已加入超时重试与明细过滤；大批量媒体建议搭配 Motrix 等下载器

## 下载

| 文件 | 说明 |
| --- | --- |
| `QZoneArchiver-4.9.0-setup.exe` | 安装版（推荐） |
| `QZoneArchiver-4.9.0-portable.exe` | 免安装版（首次启动需自解压，稍慢） |
